apiVersion: apps/v1
kind: Deployment
metadata:
  name: core-ledger
  namespace: app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: core-ledger
  template:
    metadata:
      labels:
        app: core-ledger
    spec:
      containers:
      - name: ledger
        image: asia-southeast2-docker.pkg.dev/project-3ad3e57a-ebe0-4fe5-b00/lab-images/banking:latest
        imagePullPolicy: Always
        command: ["/bin/sh", "-c"]
        args:
        - |
          # 1. Inject JSON import
          sed -i '1s/^/import json\n/' $APP_SCRIPT;
          
          # 2. Append Prometheus server initialization to the bottom of the script
          echo "" >> $APP_SCRIPT;
          echo "from prometheus_client import start_http_server" >> $APP_SCRIPT;
          # Spins up a background telemetry responder thread on the service's port right before running
          echo "try:" >> $APP_SCRIPT;
          echo "    start_http_server(int(os.environ.get('PORT', 8080)))" >> $APP_SCRIPT;
          echo "    print('Telemetry engine online')" >> $APP_SCRIPT;
          echo "except Exception as e:" >> $APP_SCRIPT;
          echo "    print('Telemetry skipped or port bound:', e)" >> $APP_SCRIPT;
          echo "" >> $APP_SCRIPT;
          
          # 3. Launch the patched script
          python3 $APP_SCRIPT;
        resources:
          requests:
            cpu: "20m"       
            memory: "64Mi"   
          limits:
            cpu: "100m"     
            memory: "128Mi"
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8082
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8082
          initialDelaySeconds: 5
          periodSeconds: 10    
        volumeMounts:
        - name: secrets-store-inline
          mountPath: "/var/secrets"
          readOnly: true
        ports:
        - containerPort: 8082
        env:
        - name: APP_SCRIPT
          value: "core-ledger.py"  
      volumes:
      - name: secrets-store-inline
        csi:
          driver: secrets-store-gke.csi.k8s.io
          readOnly: true
          volumeAttributes:
            secretProviderClass: "app-secrets-provider"

---
apiVersion: v1
kind: Service
metadata:
  name: core-ledger-service
  namespace: app
  labels:
    app: core-ledger
    prometheus.io/scrape: "true"
  annotations:
    prometheus.io/path: "/metrics"
spec:
  type: ClusterIP
  ports:
  - name: http-metrics 
    port: 8082
    targetPort: 8082
    protocol: TCP
  selector:
    app: core-ledger


---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: core-ledger-hpa
  namespace: app
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: core-ledger
  minReplicas: 1
  maxReplicas: 3
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70